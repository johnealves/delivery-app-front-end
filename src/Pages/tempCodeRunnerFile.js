brackets.forEach((char, index) => {
    if (char === "(") {
      brackets.foreach((char2, index2) => {
        if (char === ")") {
          brackets.slice(index2)
          brackets.slice(index2)
        }
      })
    }
  })